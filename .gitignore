node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Firebase 앱 설정 파일 — 공개 레포라 올리지 않음
# 로컬: 프로젝트 루트에 두고 사용 / EAS Build: 파일 환경변수(GOOGLE_SERVICES_JSON, GOOGLE_SERVICE_INFO_PLIST) — app.config.js 참고
google-services.json
GoogleService-Info.plist
functions/.env
functions/service-account.json
scripts/.env
.env
.env.local

# macOS
.DS_Store

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# ── 서비스 계정 키 (절대 커밋 금지) ──
scripts/service-account.json
scripts/*firebase-adminsdk*.json
functions/*firebase-adminsdk*.json

# ── 웹 (Next.js, web/) — web/.gitignore 도 함께 적용 ──
web/.next/
web/.next-emu/
web/e2e/shots/

# ── Firebase 에뮬레이터 로그 ──
firebase-debug.log
firestore-debug.log
ui-debug.log

# ── 빌드 결과물·로컬 전용 ──
functions/lib/
*.tsbuildinfo
web/e2e/results.json
_legacy_backup/
Claude outputs/
