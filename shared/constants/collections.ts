/**
 * Firestore 컬렉션 이름 — web / functions / scripts 에서 동일 문자열 유지
 */

/** 보호자(uid) ↔ 학생 ↔ 캠퍼스 연결 (구 enrollments) */
export const COL_GUARDIAN_LINKS = 'guardianLinks';

/** 학생이 어떤 운영 건(programRun)을 수강하는지 */
export const COL_STUDENT_PROGRAM_ENROLLMENTS = 'studentProgramEnrollments';
