# 이 파일을 .env로 복사 후 실제 값 입력 (커밋 금지)

# 해시에 사용할 고유 salt (32자 이상 랜덤 문자열 권장)
# openssl rand -hex 32  로 생성 가능
HASH_SALT=your-random-salt-here

# (선택) 명단 import·Admin 웹 — companyAdmin claim 없이 허용할 UID (쉼표 구분)
# ROSTER_IMPORT_UIDS=firebase-auth-uid-here
