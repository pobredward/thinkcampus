# E2E — 에뮬레이터 + 헤드리스 브라우저

실제 SMS·실데이터 없이, **실제 Cloud Functions 코드**(`../functions`)를 Firebase 에뮬레이터에서 돌리고 웹 앱 전체 흐름을 검증한다.

## 준비 (최초 1회)

```bash
npm i -g firebase-tools                      # Java 11+ 필요 (Firestore 에뮬레이터)
cd functions && npm install && npm run build && cd ..
cd scripts   && npm install && cd ..
cd web       && npm install && npx playwright install chromium && cd ..
```

`functions/.env` 와 `scripts/.env` 에 같은 `HASH_SALT` 가 들어 있어야 한다 (루트 README 2-2).

## 실행 (터미널 3개)

```bash
# ① 저장소 루트 — 에뮬레이터 (demo- 로 시작하는 프로젝트 id 는 실제 프로젝트에 절대 접속하지 않음)
firebase emulators:start --only auth,firestore,functions --project demo-thinkcampus

# ② 저장소 루트 — 시드 (등록코드 DS26-A3F7K 등). 다시 돌릴 때마다 --clean
cd scripts && FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 GCLOUD_PROJECT=demo-thinkcampus npx ts-node seed.ts --clean
curl -X DELETE "http://127.0.0.1:9099/emulator/v1/projects/demo-thinkcampus/accounts"   # Auth 사용자 초기화

# ③ web — 에뮬레이터용 빌드(.next-emu) + 서버(3100) 후 테스트
cd web && npm run build:emu && npm run start:emu
cd web && npm run e2e           # 창을 보면서: E2E_HEADED=1 npm run e2e
```

결과: 콘솔에 `59/59 passed`, 스크린샷은 `e2e/shots/`, 상세는 `e2e/results.json`.
등록코드 `DS26-A3F7K` 는 한 번 쓰면 "사용됨"이 되므로 다시 돌릴 때는 ②를 반복한다.

수동으로 확인하고 싶으면 ③ 대신 `npm run dev:emu` → http://127.0.0.1:3100 . OTP 는 에뮬레이터 UI(http://127.0.0.1:4000/auth) 또는
`curl http://127.0.0.1:9099/emulator/v1/projects/demo-thinkcampus/verificationCodes` 에서 확인.

## 체험 모드 (별도, 에뮬레이터 불필요)

```bash
cd web && npm run build:demo && npm run start:demo     # .next-demo · 포트 3200
npm run e2e:demo                                        # 9/9 passed
```

`/` → 바로 메인(등록코드 없음) · `/onboarding`·로그인 화면도 메인으로 · 010-7656-7933 계정(보호자 신선웅 — 인사말 "환영합니다, 신선웅 학부모님" · 이름 묻는 카드 없음 · 맨 위 경로 [홈], 자녀 신민준·신서연 전환해도 인사말 그대로) · 회차·안내 페이지(상단 경로로 오가기 · 일시 및 장소의 오시는 길) · 주소로 바로 열기 · 리포트 공유는 서버 없이 링크 복사 · 내 정보(이름·번호·자녀·관계·캠퍼스, [이름 수정]은 안내만) · 알림 문구 학생 이름도 신민준 · 로그아웃/보호자 초대/회원 탈퇴는 막고 안내만 · **Firebase 로 나간 요청 0건** · 페이지 오류 없음

## 검증 시나리오 (59)

온보딩: 루트 리다이렉트 · 코드 자동 포맷 · 없는/사용된/만료 코드 안내 · 정상 코드 미리보기(캠퍼스·마스킹 이름) · **보호자 이름 칸(관계 아래·전화번호 위, 비우면 버튼 꺼짐, 1자/숫자 안내)** · 생년월일 불일치(시도 횟수) · 생년월일 포맷 · redeemCode → OTP(전화번호 URL 비노출) · OTP → 커스텀토큰 로그인 → 홈(가입 때 입력한 이름으로 "환영합니다, 박지영 학부모님", 에뮬레이터 계정 displayName 확인)

메인: 자녀 1명일 때 전환 버튼 숨김 · 맨 위 상단 경로 [홈] · 인사말 한 줄("환영합니다, 박지영 학부모님" — 보호자 이름) · 간소화된 홈 카드(3/6회) → 프로그램 화면: **상단 경로 `홈 › 토요 창의융합`** · 헤더에 일시·장소 없음 · 순서 **[수업 규정·지침 · 필독](맨 위 카드)** → 수업 안내 → 회차별 수업 → 종합 리포트 · 수업 안내 4줄(**프로그램 일시 및 장소가 첫 줄**, 한 줄에 하나, 제목만) · "총 6회 · 진행 3회" · 회차 버튼 3개씩 2줄(첫 화면 안, "N회차"만 — 날짜·출결 없음, 끝난 1~3회차 가라앉은 면 ✓, 4회차 다음 수업 골드, 남은 회차 카드; 버튼 모양은 바탕과 다른 색의 테두리로 확인) · 종합 리포트는 6회 끝나면 열림 · 이모지 없음 · **일시 및 장소 페이지(요일·시간·기간·장소 · 장소 · 오시는 길 — 지도 링크(네이버 지도) · 주차 · 도착하면 · 데려갈 때, 회차별 날짜 없음) → 경로 [토요 창의융합] 로 복귀(방문 기록이 늘지 않음)** · 3회차(지각) 출결 탭(입실·늦은 시간, 탭 5개 글자만) · 일정 탭(`?tab=schedule`) · 내용 탭 · Q&A 펼치기 · 챗봇 버튼만(전화 링크 없음) · 리포트 탭(선생님 한마디·참여·평가) · 새로고침 후 탭 유지 · 다음 회차(예정) → 리포트 탭 없음·일정 탭부터·출결 "수업 전" · 이전/다음 회차 이동 시 탭 유지 · 경로 `홈 › 토요 창의융합 › N회차` → **[토요 창의융합] 한 번에 복귀(새로고침 뒤에도 기록 안 쌓임)** · Q&A → 챗봇 → **고객 지원 경로 `홈 › 토요 창의융합 › 4회차 › 고객 지원`(주소에 from=session) → [4회차](Q&A 탭) → [토요 창의융합] → [홈]** · 수강 중 공지·규정(필독) → 독립 페이지(항목 탭 없음, 경로 `… › 공지사항`/`… › 수업 규정·지침`) → [토요 창의융합] · 홈에 수강 예정 섹션 없음(잠시 숨김) → 수강 예정 화면(주소로 열기) 같은 구성(경로 `홈 › 겨울방학 STEAM 특강` · 규정 맨 위 · 일시 및 장소 첫 줄 · 수강 예정 배지·첫 수업 D-day·총 6회, 진행·리포트 없음) · 수강 예정 일시 및 장소(휴강 · 2층 창작실 · 오시는 길, 회차별 날짜 없음) → 6회차 탭 3개(출결 없음) → 경로로 복귀 · 안내 페이지를 하나씩(규정 필수 2개 · Q&A 프로그램 질문 먼저·펼침·전화 없음 · 공지 준비물 · 목적), 매번 경로 [겨울방학 STEAM 특강] → [홈] · 홈에 규정 버튼 없음 · 이전 수강 이력은 맨 아래 버튼 → 목록 화면(경로 `홈 › 이전 수강 이력`) → **지난 프로그램(경로 `홈 › 이전 수강 이력 › 여름학기`) → [이전 수강 이력] → [홈]** · 홈에 샘플 리포트 카드 없음 · 종합 리포트(주소로 열기) 공유(클립보드·토스트) · **종합 리포트 경로 `홈 › 토요 창의융합 › 종합 리포트` → 기록이 없으니 [토요 창의융합] 은 이동** · 예전 주소 리디렉션(/attendance, /main/program/session/sess-02 → 내용 탭·수업 자료) · 알림 모두 읽음 · 내 정보(이름·번호) → [이름 수정] 시트(1자 안내 → 박서윤 저장 → 토스트 · 홈 인사말 바로 바뀜) · 보호자 초대(addGuardianPhone) · FAQ/리포트/출결/리포트 상세/프로그램 목록 렌더 · 챗봇 응답 · 딥링크(회차 화면) 새로고침 시 세션 유지 + 경로 `홈 › 토요 창의융합 › 2회차` → [토요 창의융합] 로 이동 · 인쇄 호출

인증: 로그아웃 → 온보딩 · 미로그인 딥링크 → 로그인(?next) · 로그인 후 원래 화면 복귀 · 기존 계정에 두 번째 자녀 연결(DS26-ABC12, 이미 이름이 있어 가입 화면 입력으로 바뀌지 않음) → 홈 자녀 전환 버튼(상단 경로와 같은 줄 오른쪽)·시트·새로고침 후 유지 · 전환해도 인사말은 보호자 이름 그대로 · 전환한 자녀의 출결이 회차 화면에 반영(2회차 결석 → 리포트 탭에 평가 없음) · 잘못된 OTP · 미등록 번호 안내 · **초대받은 보호자 로그인 → 자동 연결 → 이름이 없어 "환영합니다, 학부모님" + 이름 묻는 카드 → [나중에](새로고침해도 다시 묻지 않음) → 표시를 지우면 다시 → 1자 안내 → 최은주 저장 → 인사말 바로 바뀜 · 계정 displayName 확인** · **회원 탈퇴**(안내에 자녀·번호, 확인 체크 전 버튼 비활성, 취소/탈퇴 → /goodbye) · 탈퇴 후 서버 정리(Auth 계정·enrollment 삭제, 학생 보호자/초대 목록에서 제거, 원래 보호자 유지, 탈퇴 기록에 개인정보 없음 — 에뮬레이터 REST 로 확인) · 같은 번호로 재로그인해도 자동 연결 안 됨 · 로그인 상태로 /goodbye → 홈(다시 로그인해도 바꾼 이름 박서윤 그대로) · 데스크톱 480px 프레임

공통: **모든 스크린샷 시점에 화면에 보이는 글자를 전부 검사해 14px 미만이 하나라도 있으면 실패** (마지막 시나리오 "모든 화면 글자 크기 14px 이상").

탈퇴 확인에 쓰는 에뮬레이터 관리자 조회(`Authorization: Bearer owner`)는 에뮬레이터에서만 동작한다.

콘솔에 보이는 `404/409/504/401/400` 은 일부러 넣은 오류 케이스의 응답이고, `501 recaptchaConfig` 는 에뮬레이터가 reCAPTCHA Enterprise 설정 API 를 구현하지 않아서 나오는 것(무해)이다.
